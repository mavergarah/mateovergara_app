version: "2"
services:
  web:
    build: .
    ports:
      - "8000:8000"
  nginx:
    image: nginx
    ports:
      - "5000:5000"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
